<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.duyntkd.finalprojectmobile">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AbstractUserActivity"></activity>
        <activity android:name=".SelfTaskEditActivity" />
        <activity android:name=".TaskAssignmentActivity" />
        <activity android:name=".TaskConfirmationActivity" />
        <activity android:name=".TaskDetailForManagerActivity" />
        <activity android:name=".TaskDetailHistoryActivity" />
        <activity android:name=".TaskDetailActivity" />
        <activity android:name=".ManagerActivity" />
        <activity android:name=".EmployeeActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>